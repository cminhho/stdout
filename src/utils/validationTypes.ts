/**
 * Re-export shared validation types from @/types/validation for backward compatibility.
 */
export type { ParseError, ValidationResult } from "@/types/validation";
export { singleErrorToParseErrors } from "@/types/validation";
